#include<stdio.h>
int main()
{
  int a,c,b;
  scanf("%d%d%d",&a,&b,&c);
  if(a>b)
  {
   printf("a is larger than b&c");
  }
  else if(b>c)
  {
     printf("b is larger than c&a");
  }
  else
  {
       printf("c is larger than b&a");
  }
}
